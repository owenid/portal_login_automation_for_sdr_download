{
  "name": "portal-login-automation-for-sdr-download",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda that logs into the Gradwell SSO/Admin portal on a monthly schedule and downloads the SDR CDR file to S3.",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@sparticuz/chromium": "^153.0.0",
    "playwright-core": "^1.63.0"
  }
}
